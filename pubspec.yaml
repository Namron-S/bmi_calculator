name: bmi_calculator
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+2

environment:
  sdk:  '>=3.3.0-210.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "/Users/norman/Documents/proggen/Flutter/bmi_calculator/assets/icon/weight-scale.png"
  #flutter_launcher_icons doesn't work with *.svg. But I  kept weight-scale.svg nonetheless in the assets folder.

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true